// TypeScript is modular, we can divide our code up over several files
// We can export a class, interface, variable by adding 'export' keyword in front of it

export class ExportedClass {
    // This class will be exported...
}