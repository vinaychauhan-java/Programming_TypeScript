// Generics are types which can hold several types

// Consider the Array object
let numberArray: Array<number>; // This array will accept number's only

//numberArray = ['Test-1','Test-2']; // => Error
numberArray = [1, 2, 3];